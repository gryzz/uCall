        var currentUser = {
            username: "{{user.username}}",
            firstName: "{{user.first_name}}",
            lastName: "{{user.last_name}}",
            agentId: "{{user.get_profile.agentKey}}",
            isAdmin: '{{user.is_staff|yesno:"true,false"}}'
        };
